{
  "name": "pantheon-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Pantheon Studio — sitio web (Cinematic Editorial)",
  "scripts": {
    "build": "node build.js && tailwindcss -i ./src/input.css -o ./site/assets/tw.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.11",
    "sharp": "^0.35.3",
    "tailwindcss": "^3.4.17"
  }
}
